import styles from './LoadingSpinner.module.css'

const LoadingSpinner = () => {
    return (
/*       <div className={styles.container}>
        <div className={styles.spinner}></div>
      </div> */ <p>LOAD LOAD LOAD</p>
    );
  };
  
  export default LoadingSpinner;